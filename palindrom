#include<stdio.h>
void main()
{
	int num,n,reverce=0,reminder;
	printf("enter the number");
	scanf("%d",&num);
	n=num;
	while(n>0)
	{
		reminder=n%10;
		reverce=(reverce*10)+reminder;
		n=n/10;
	}
		if(num==reverce)
			printf("palindrom");
		else
			printf("not palindrome");
}
