#include<stdio.h>
int main() {
int a,b;

    printf("\tEnter two integers :");

            scanf("%d%d", &a,&b);
    
    printf("\nSum of these two numbers are %d", a+b);
    
    printf("\nDiff of these two no. are %d", a-b);

}
    return0;
