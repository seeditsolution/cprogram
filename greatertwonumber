#include<stdio.h>
#include<conio.h>
void main()
{
   int a,b;
   clrscr();
   scanf("%d %d",&a,&b);
   if(a>b)
   {
      printf("a is greater number %d",a);
   }
   else
   {
     printf("b is greater number % d",b);
   }
   getch();
}
